/**
 * Nome do evento emitido em `window` após login bem-sucedido.
 * O plus-shell escuta o mesmo literal (manter sincronizado com `plus-shell/src/shellAuthEvents.js`).
 */
export const PLUS_AUTH_LOGIN_SUCCESS_EVENT = "plus-auth-login-success";
